const IMAGES = {
    load: function() {

        // Sprites
        game.load.image('link', CONSTANTS.images.link);

        // Tilemaps
        game.load.image('tiles', CONSTANTS.images.tiles);

        // Various
        game.load.image('title', CONSTANTS.images.title);
    }
};
